<!-- Generated by: vrp-ir audit examples/sample-usg-risky.cfg --lang zh -->
# 安全验收报告

- **配置文件**: `examples/sample-usg-risky.cfg`
- **结论**: ❌ 未通过
- **汇总**: 3 未通过 · 2 告警 · 0 通过

## 检查项

### ❌ `FW-DEFAULT-DENY` [CRITICAL] — 安全策略默认动作应为 deny(拒绝未匹配流量)

默认动作为 permit:所有未匹配安全策略的流量被放行(permit-any)。

**证据**:
- `examples/sample-usg-risky.cfg:14` — `default action permit`

### ❌ `FW-RULE-ZONE-SCOPE` [HIGH] — permit 规则应有源/目的安全区域约束

规则 'any-to-any' 放行流量但缺少 source-zone, destination-zone,匹配所有区域。

**证据**:
- `examples/sample-usg-risky.cfg:15` — `rule name any-to-any`

### ❌ `FW-ZONE-IFACE-UNIQUE` [HIGH] — 每个接口应仅绑定到一个安全区域

接口 GigabitEthernet0/0/1 被绑定到 2 个区域:trust, untrust。

**证据**:
- `examples/sample-usg-risky.cfg:7` — `add interface GigabitEthernet0/0/1`
- `examples/sample-usg-risky.cfg:11` — `add interface GigabitEthernet0/0/1`

### ⚠️ `FW-RULE-LOGGING` [MEDIUM] — permit 规则应开启会话日志(session logging)

规则 'any-to-any' 放行流量但未开启 session logging,可审计性不足。

**证据**:
- `examples/sample-usg-risky.cfg:15` — `rule name any-to-any`

### ⚠️ `FW-RULE-LOGGING` [MEDIUM] — permit 规则应开启会话日志(session logging)

规则 'web-in' 放行流量但未开启 session logging,可审计性不足。

**证据**:
- `examples/sample-usg-risky.cfg:17` — `rule name web-in`
